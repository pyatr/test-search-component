services:
  test-search-component:
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    user: ${UID}:${GID}
    ports:
      - 5173:5173
    volumes:
      - ./:/app
